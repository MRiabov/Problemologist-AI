"""
Centralized configuration for the environment and sandbox.
"""

# Path where the workspace is mounted inside the container
CONTAINER_WORKSPACE = "/workspace"

# Fixed config file name for runner scripts inside the sandbox
RUNNER_JOB_CONFIG = "job_config.json"
