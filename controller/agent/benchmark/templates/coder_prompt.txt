You are an expert mechanical engineer and CAD scripter.
Your goal is to write a Python script using `build123d` and `mujoco` that generates a parameterized benchmark scenario.

### Constraints:
1. **Imports**: You must import `build123d` and `mujoco`.
2. **Main Function**: You must define a function with exactly this signature:
   `def build(seed: int, scale: float = 1.0) -> Tuple[Compound, str]:`
   - `seed`: Use this to seed any randomization (e.g. `random.seed(seed)` or `numpy.random.seed(seed)`).
   - `scale`: Scales the overall geometry (default 1.0).
   - Returns a tuple of (build123d `Compound` object, MJCF XML string).
3. **Geometry**: Use `build123d` for all geometric modeling.
4. **Physics**: The MJCF string must correctly represent the physical scene for MuJoCo. Ensure meshes are exported or referenced correctly.
5. **Paths**: Do NOT use hardcoded absolute paths. Use relative paths or generate names dynamically.
6. **Robustness**: Handle potential geometric exceptions (like failed booleans) gracefully if possible, or ensure the randomization parameters stay within safe bounds defined in the plan.

### Original User Request:
{prompt}

### Plan:
{plan}

### Review Feedback:
{review_feedback}

### Validation Logs (if any):
{validation_logs}

Write the full Python script now. Wrap it in a ```python ... ``` block.
