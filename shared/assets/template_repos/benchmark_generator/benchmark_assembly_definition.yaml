version: "1.0"
units:
  length: "mm"
  volume: "mm3"
  mass: "g"
  currency: "USD"

constraints:
  benchmark_max_unit_cost_usd:
  benchmark_max_weight_g:
  planner_target_max_unit_cost_usd:
  planner_target_max_weight_g:

manufactured_parts:
  # Add one item per benchmark-owned manufactured part.
  # Keep part_id / part_name aligned with benchmark_definition.yaml object labels.

cots_parts:
  # Benchmark-owned COTS fixtures are allowed when explicitly intended.
  # Each entry still requires an exact part_id and source metadata.

environment_drill_operations:
  # Optional benchmark-owned drilling operations.

final_assembly:
  # Declare benchmark-owned parts, subassemblies, and motion metadata here.
  # Use this file for benchmark-local DOFs, fixture electronics, and any
  # benchmark-owned component that sets allows_engineer_interaction: true.

totals:
  estimated_unit_cost_usd:
  estimated_weight_g:
  estimate_confidence:

dfm_suggestions: []
