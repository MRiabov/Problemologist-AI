---
description: Deprecated workflow file. Use the skill instead.
---

This workflow has been migrated to a skill:

- `.codex/skills/commit-split-workflow/SKILL.md`

Manual trigger only: invoke it explicitly by name (`$commit-split-workflow`) when needed.
