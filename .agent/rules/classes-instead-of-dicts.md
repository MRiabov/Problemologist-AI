---
trigger: always_on
---

I prefer more strict, typed classes instead of freeform dicts for data exchange. This is because we can easily add observability to it, as we already store things to a local DB for observability, and also maintains code hygiene and interpretability.
