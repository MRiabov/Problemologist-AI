transcript:
- node: engineer_planner
  steps:
  - thought: Run a long deterministic command so the interrupt endpoint can cancel
      active execution.
    tool_name: execute_command
    tool_args:
      command: import time; time.sleep(20); print('INT-030-running')
    expected_observation: INT-030-running
  - finished: true
    summary: INT-030 planner completed.
