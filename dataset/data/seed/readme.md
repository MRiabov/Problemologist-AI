# Seed dataset folder

Just to clarify: seed datasets are (should be!) highly curated datasets that can be later used as either examples or as seeds for other benchmarks to run.

Some role-based rows may also be duplicated into mode-specific variants such as
`ec-001-drawing-full`. Those copies exist so drawing-policy changes can be
evaluated without overwriting the legacy base row ids.
